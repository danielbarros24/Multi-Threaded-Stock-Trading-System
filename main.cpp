#include "loginSystem/loginSystem.h"
#include <iostream>
#include <string>

using namespace std;

int main() {
		
    if (loginSystem()){
			printf("ENTERING IN APPLICATION");
		}
		else {
			while(1);
		}
    return 0;   
}